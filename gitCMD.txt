бнor create a new repository on the command line

echo # zion >> README.md
git init
git add README.md
git commit -m "first commit"
git remote add origin https://github.com/zerubbabel/zion.git
git push -u origin master

бнor push an existing repository from the command line

git remote add origin https://github.com/zerubbabel/zion.git
git push -u origin master

бнor import code from another repository

You can initialize this repository with code from a Subversion, Mercurial, or TFS project.

git pull origin master

差异打包:
$ git diff 608e120 4abe32e --name-only | xargs zip ../update.zip

branch
 git checkout -b newBranch